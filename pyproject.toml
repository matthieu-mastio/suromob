[project]
name = "popsynth"
version = "0.1.0"
description = "Scripts for population synthesis"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
    "scipy",
    "smt",
]
